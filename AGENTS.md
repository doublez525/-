修改规范

- 文案使用简体中文。
- 任何用户输入写入页面时优先使用 `textContent`；若确实需要拼接 HTML，必须先使用 `escapeHtml()`，不得新增未转义的渲染路径。
- 复用 CSS 变量（如 `--accent`、`--ink`、`--muted`）作为主题单一来源；兼顾 `520px` 及以下的布局与可操作性。
- 保留表单标签、`aria-live`、`aria-hidden` 和原生按钮键盘操作。
- 动画启动、重置或重复触发时，必须清理相关定时器和延迟任务，避免残留动画或资源泄漏。

验证

- 验证空白输入提示和正常输入后祝福内容、爱心动画是否正确显示。
- 验证重复提交不会叠加定时器；点击“再写一个名字”后不应继续生成残余爱心。
- 验证输入内容以纯文本显示，不会被当作 HTML 解析。
- 验证桌面端和不大于 `520px` 的窄屏布局、表单与按钮可操作性。
