修改规范

- 文案使用简体中文。
- 页面祝福对象固定为“zz”；如需更改对象或文案，须同步检查标题、首屏文案、祝福文案与页面标题。
- 复用 CSS 变量（如 `--accent`、`--ink`、`--muted`）作为主题单一来源；兼顾 `520px` 及以下的布局与可操作性。
- 保留语义化结构、`aria-live`、`aria-hidden` 和原生按钮键盘操作。
- 动画启动、重置或重复触发时，必须清理相关定时器和延迟任务，避免残留动画或资源泄漏。

验证

- 验证点击“点亮爱心”后，固定的 zz 祝福内容与爱心动画正确显示。
- 验证重复点击不会叠加定时器；点击“再看一次”后不应继续生成残余爱心。
- 验证点击“再看一次”后返回首屏，焦点回到“点亮爱心”按钮。
- 验证桌面端和不大于 `520px` 的窄屏布局、按钮可操作性。
