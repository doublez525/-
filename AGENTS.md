修改规范

- 文案和日期使用简体中文。
- 任何用户输入进入 HTML 前必须使用 `escapeHtml()`；不得新增未转义的渲染路径。
- 复用 CSS 变量（如 `--accent`、`--ink`、`--muted`）作为主题单一来源；兼顾 `520px` 及以下的布局与可操作性。
- 保留表单标签、`aria-label`、任务列表 `aria-live` 和原生按钮键盘操作。

验证

- 验证空白/正常新增、完成切换、筛选、删除、清除已完成，以及计数、摘要和进度环同步。
- 验证双击编辑后的 Enter、失焦和 Escape 行为。
- 验证刷新后的任务与自定义鼓励语持久化；清除站点数据后恢复示例任务。
- 验证桌面端和不大于 `520px` 的窄屏布局。
